import { Container, Typography } from "@mui/material";

const TopArea = () => {
    return (
        <Container>
            <Typography variant="h4">Top Area Section</Typography>
        </Container>
    );
};

export default TopArea;
